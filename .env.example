# Example environment file for `notification_service`.
# DO NOT commit real secrets. Replace placeholder values with secrets in your environment.

DJANGO_SECRET_KEY=replace-with-a-secure-secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
GATEWAY_URL=http://localhost:8000

# Database
NOTIFICATIONS_DB_NAME=test_notifications_db
DB_USER=postgres
DB_PASSWORD=postgres_password_here
DB_HOST=postgres
DB_PORT=5432

# Redis (Celery broker / Channels)
REDIS_URL=redis://redis:6379/1

# Auth service
AUTH_SERVICE_URL=http://auth_service:8001
AUTH_SERVICE_TIMEOUT=10

# Kafka (for local/dev tests) - set to your broker address
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# Third-party provider placeholders
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
FIREBASE_CREDENTIALS_JSON_PATH=/path/to/firebase/credentials.json

# Notes:
# - Use `.env` locally (gitignored) for real secrets. Keep this example file committed.
# - For CI, inject secrets via GitHub Actions Secrets or your CI provider.
